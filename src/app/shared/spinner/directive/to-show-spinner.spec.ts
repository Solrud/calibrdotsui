import { ToShowSpinner } from './to-show-spinner.directive';

describe('Spinner', () => {
  it('should create an instance', () => {
    const directive = new ToShowSpinner();
    expect(directive).toBeTruthy();
  });
});
