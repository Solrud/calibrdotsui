import { ToShowToast } from './to-show-toast.directive';

describe('Toast', () => {
  it('should create an instance', () => {
    const directive = new ToShowToast();
    expect(directive).toBeTruthy();
  });
});
